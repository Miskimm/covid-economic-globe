// Launcher: run from repo root with `node js/server.js`
// Serves the final prototype at http://localhost:3000
const path = require("path");
require(path.join(__dirname, "..", "final prototype", "js", "server.js"));
